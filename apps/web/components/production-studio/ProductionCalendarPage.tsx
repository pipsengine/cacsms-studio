"use client";
import { ChevronLeft, ChevronRight } from "lucide-react";
import { useMemo, useState } from "react";
import styles from "./ProductionStudio.module.css";
export function ProductionCalendarPage(){const [month,setMonth]=useState(()=>new Date());const days=useMemo(()=>{const first=new Date(month.getFullYear(),month.getMonth(),1);const count=new Date(month.getFullYear(),month.getMonth()+1,0).getDate();return [...Array(first.getDay()).fill(null),...Array.from({length:count},(_,index)=>index+1)]},[month]);return <section className={styles.page}><header className={styles.header}><div><div className={styles.breadcrumb}>Production Studio / Production Calendar</div><h1 className={styles.title}>Production Calendar</h1><p className={styles.subtitle}>Coordinate milestones, shoots, recordings, reviews, approvals, delivery dates, and publishing schedules.</p></div><div className={styles.actions}><button className={styles.button} aria-label="Previous month" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))}><ChevronLeft size={14}/></button><button className={styles.control}>{month.toLocaleString(undefined,{month:"long",year:"numeric"})}</button><button className={styles.button} aria-label="Next month" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))}><ChevronRight size={14}/></button><button className={styles.primary}>New Event</button></div></header><section className={styles.calendarLayout}><article className={styles.card}><div className={styles.calendar}>{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(day=><strong className={styles.dayName} key={day}>{day}</strong>)}{days.map((day,index)=><div className={styles.calendarDay} key={index}>{day?<><strong>{day}</strong><div className={styles.meta}>No events</div></>:null}</div>)}</div></article><aside className={styles.list}>{["Upcoming Events","Schedule Conflicts"].map(title=><article className={styles.card} key={title}><div className={styles.sectionHeader}><div className={styles.sectionTitle}>{title}</div></div><div className={styles.empty}>{title==="Upcoming Events"?"No upcoming events.":"No conflicts detected."}</div></article>)}</aside></section></section>}
