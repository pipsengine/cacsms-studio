import { NextResponse } from "next/server";
import { productionPages } from "@/lib/production-studio-pages";
import type { CreateProductionPayload } from "@/types/production-studio";
export const runtime="nodejs"; export const dynamic="force-dynamic";
const supported=new Set([...Object.keys(productionPages),"production-pipeline","production-calendar"]);
export async function GET(_request:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;const correlationId=crypto.randomUUID();if(!supported.has(slug))return NextResponse.json({message:"Production Studio endpoint not found.",correlationId},{status:404});const config=productionPages[slug];return NextResponse.json({generatedAt:new Date().toISOString(),slug,metrics:(config?.metricLabels??[]).map((label,index)=>({key:`${slug}-${index}`,label,value:"0",helper:"No production data yet"})),records:[],panels:{},total:0},{headers:{"x-correlation-id":correlationId,"Cache-Control":"no-store"}})}
export async function POST(request:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(slug!=="create-production")return NextResponse.json({message:"Action not supported."},{status:405});let payload:Partial<CreateProductionPayload>;try{payload=await request.json()}catch{return NextResponse.json({message:"A valid JSON body is required."},{status:400})}if(!payload.title?.trim()||!payload.topic?.trim()||!payload.contentType?.trim())return NextResponse.json({message:"Title, topic, and content type are required."},{status:400});return NextResponse.json({id:crypto.randomUUID(),status:"draft",createdAt:new Date().toISOString()},{status:201})}
