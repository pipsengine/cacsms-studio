# [OPEN] image-render-persist

## Summary
- Issue: `http://localhost:3008/visuals/image-generator` loads, but no new persisted asset is produced.
- Current known state:
- Auth bootstrap is fixed.
- Storyboard editor is fixed.
- Local image daemon no longer fails immediately with the prior OpenCV `CascadeClassifier` crash.
- Direct daemon smoke render reaches active execution on CPU.

## Hypotheses
- H1: The daemon finishes rendering, but the app does not persist or attach the generated bytes to the active variant.
- H2: The app is marking the in-flight render stale or rejected before the CPU render completes.
- H3: Validation or asset-quality gates reject the newly generated bytes after render completion.
- H4: The UI is mainly reflecting prior rejected variants while a newer render attempt is still active.

## Evidence
- To be collected.

## Next Steps
- Add instrumentation only after the current hypotheses are validated against the live scheduler, daemon health, and persistence path.
